// Package common contains sub packages which might be shared amongst other Dymension repos, but which aren't utils. For example, common types
// which might be sent in an ibc message between the rollapp and the hub, and so on.
// This package should not contain files, but only sub packages.
package common
